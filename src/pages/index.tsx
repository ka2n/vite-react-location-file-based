import { Link, Navigate, useNavigate } from '@tanstack/react-location';
import { Nav } from '../Nav';

const TopPage = () => {
  return (
    <div>
      Top
      <hr />
      <Nav />
    </div>
  );
};

export default TopPage;
